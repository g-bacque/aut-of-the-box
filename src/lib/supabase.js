// Cliente de Supabase. En la Fase 1 todavía no se usa (trabajamos con datos
// de ejemplo en src/data/mock.js). Se activará en la Fase 2.
//
// Para activarlo:
//   1. npm install @supabase/supabase-js
//   2. Rellenar .env.local con VITE_SUPABASE_URL y VITE_SUPABASE_ANON_KEY
//   3. Descomentar el bloque de abajo.

// import { createClient } from '@supabase/supabase-js';
//
// export const supabase = createClient(
//   import.meta.env.VITE_SUPABASE_URL,
//   import.meta.env.VITE_SUPABASE_ANON_KEY
// );

export const supabase = null;
